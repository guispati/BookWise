import { styled } from "@/styles";

export const PageTitleContainer = styled('header', {
    display: 'flex',
    alignItems: 'center',
    gap: '$3',
    marginBottom: '$10',

    svg: {
        color: "$green100",
    },
});