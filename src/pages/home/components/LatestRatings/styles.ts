import { styled } from "@/styles";

export const LatestRatingsContainer = styled('section', {
    section: {
        marginTop: '$4',
        display: 'flex',
        flexDirection: 'column',
        gap: '$3',
    },
});