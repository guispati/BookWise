import { styled } from "@/styles";

export const ProfileRatingsContainer = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '$8',
});

export const ProfileRatingList = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '$6',
});